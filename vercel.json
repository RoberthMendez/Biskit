{
  "version": 2,
  "builds": [
    { "src": "Biskit/package.json", "use": "@vercel/static-build", "config": { "distDir": "dist/biskit" } }
  ],
  "routes": [
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
